import { WeekFormatPipe } from './week-format.pipe';

describe('WeekFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
