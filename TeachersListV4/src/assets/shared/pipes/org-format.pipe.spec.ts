import { OrgFormatPipe } from './org-format.pipe';

describe('OrgFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OrgFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
