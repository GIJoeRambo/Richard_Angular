import { CommandFormatPipe } from './command-format.pipe';

describe('CommandFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
